function renderRooms() { const q = (roomSearch?.value || '').toLowerCase(), f = document.querySelector('.filter.active')?.dataset.filter || 'all'; const list = Campus.rooms.filter(r => (f === 'all' || f === 'occupied' && r.occupied || f === 'empty' && !r.occupied || f === 'warning' && r.warning) && r.id.toLowerCase().includes(q)); roomGrid.innerHTML = list.length ? list.map(r => `<article class="card room-card" data-id="${r.id}"><div class="room-top"><div><h3>${r.id}</h3><p class="muted">${r.floor} · ${r.block}</p></div>${badge(r.warning ? 'Warning' : r.occupied ? 'Occupied' : 'Empty', r.warning ? 'warning' : r.occupied ? 'occupied' : 'empty')}</div><div class="room-details"><div>Temperature<strong>${r.temperature}°C</strong></div><div>Humidity<strong>${r.humidity}%</strong></div><div>Appliances<strong>${r.light ? 'Light ' : ''}${r.fan ? 'Fan ' : ''}${r.ac ? 'AC' : '' || 'All off'}</strong></div><div>Current Power<strong>${r.power.toFixed(2)} kW</strong></div></div></article>`).join('') : '<div class="card muted">No rooms match this filter.</div>'; document.querySelectorAll('.room-card').forEach(x => x.onclick = () => showDetail(x.dataset.id)) }
function showDetail(id) { const r = Campus.rooms.find(x => x.id === id); shell(`Room ${r.id}`, `${r.floor} · ${r.block}`, `<div class="detail"><a class="back" href="rooms.html">← Back to rooms</a><div class="card"><div class="section-title"><div><h2>ROOM ${r.id}</h2><p class="muted">${r.floor} · ${r.block}</p></div>${badge(r.warning ? 'Warning' : r.occupied ? 'Occupied' : 'Empty', r.warning ? 'warning' : r.occupied ? 'occupied' : 'empty')}</div><div class="room-details"><div>Temperature<strong>${r.temperature}°C</strong></div><div>Humidity<strong>${r.humidity}%</strong></div><div>Light<strong>${r.light ? 'ON' : 'OFF'}</strong></div><div>Fan<strong>${r.fan ? 'ON' : 'OFF'}</strong></div><div>AC<strong>${r.ac ? 'ON' : 'OFF'}</strong></div><div>Current Power<strong id="powerRead">${r.power.toFixed(2)} kW</strong></div><div>Today's Energy<strong>${r.energyToday.toFixed(2)} kWh</strong></div></div></div><div class="card" style="margin-top:16px"><h2>Simulated Appliance Controls</h2>${['light', 'fan', 'ac'].map(k => `<div class="toggle-row"><strong>${k.toUpperCase()}</strong><button class="switch ${r[k] ? 'on' : ''}" data-device="${k}"></button></div>`).join('')}<p class="muted">Frontend demonstration only — no physical appliances are connected.</p></div></div>`); document.querySelectorAll('[data-device]').forEach(b => b.onclick = async () => { b.disabled = true; const updatedRoom = await SimulationEngine.toggleAppliance(r.id, b.dataset.device); b.disabled = false; if (!updatedRoom) { toast('Device update failed'); return; } document.querySelectorAll('[data-device]').forEach(control => control.classList.toggle('on', !!updatedRoom[control.dataset.device])); powerRead.textContent = `${Number(updatedRoom.power || 0).toFixed(2)} kW`; toast(`${b.dataset.device.toUpperCase()} state updated`) }) }
window.addEventListener('campus-state-change', () => { if (page === 'rooms' && !new URLSearchParams(location.search).get('room')) renderRooms() });
if (page === 'rooms') { const selected = new URLSearchParams(location.search).get('room'); if (selected) showDetail(selected); else { shell('Rooms', 'Campus 25 live simulated room monitoring.', `<div class="room-toolbar"><input id="roomSearch" class="select" placeholder="Search Campus 25 room ID"><div class="filters">${['all', 'occupied', 'empty', 'warning'].map((f, i) => `<button data-filter="${f}" class="btn filter ${i === 0 ? 'active' : ''}">${f[0].toUpperCase() + f.slice(1)}</button>`).join('')}</div></div><div id="roomGrid" class="room-grid"></div>`); roomSearch.oninput = renderRooms; document.querySelectorAll('.filter').forEach(b => b.onclick = () => { document.querySelectorAll('.filter').forEach(x => x.classList.remove('active')); b.classList.add('active'); renderRooms() }); renderRooms() } }
